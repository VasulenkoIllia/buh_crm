import { Vault } from "./vault";

/**
 * **Secrets** (secrets.md §15): the whole vault the reader may open — My secrets, Company, every
 * client's list and the Trash — with one search box over all of it.
 */
export function SecretsPage() {
  return <Vault />;
}
